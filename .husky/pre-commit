# #!/usr/bin/sh


# . "$(dirname "$0")/_/husky.sh"

# npm run test
# npx lint-staged 

#!/bin/sh

# Run ESLint
npm run lint

# Check ESLint exit code
if [ $? -ne 0 ]; then
  echo ""
  echo "ESLint errors or warnings found. Aborting commit."
  exit 1
fi
